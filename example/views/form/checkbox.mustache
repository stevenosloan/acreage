<div>
  {{# label }}<label>{{ . }}</label>{{/ label }}
  {{# boxes }}
  <div class="field-row">
    <input type="checkbox" class="form-checkbox" value="{{ value }}" name="{{ name }}" id="{{ id }}" {{# checked }}checked="true"{{/ checked }} />
    <label class="form-inline-label" for="{{ id }}">{{ label }}</label>
  </div>
  {{/ boxes }}
  {{# validated }}{{^ valid }}
    <div><ul>
      {{# errors }}<li>{{ getMessage }}</li>{{/ errors }}
    </ul></div>
  {{/ valid }}{{/ validated }}
</div>
